import { InfiteScrollDirective } from './infite-scroll.directive';

describe('InfiteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new InfiteScrollDirective();
    expect(directive).toBeTruthy();
  });
});
